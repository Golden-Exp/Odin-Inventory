<body>
  <div class="pokemon-grid">
    <% for (let i = 0; i < pokemon.length; i++) { %>
      <div class="pokemon-card">
        <img src="<%= pokemon[i].sprite %>" alt="sprite of <%= pokemon[i].pokemon %>">
        <h2><%= pokemon[i].pokemon %></h2>
        <p><strong>Type 1:</strong> <%= pokemon[i].type %></p>
        <p><strong>Type 2:</strong> <%= pokemon[i].type2 %></p>
        <p><strong>Region:</strong> <%= pokemon[i].region %></p>
        <p><strong>Trainer:</strong> <%= pokemon[i].trainer %></p>
      </div>
    <% } %>
  </div>

  <div style="text-align: center;">
    <button class="add-button" onclick="window.location.href='/new'">Add New Pokémon</button>
  </div>
</body>
<style>
  body {
    font-family: 'Segoe UI', sans-serif;
    background-color: #f0f2f5;
    padding: 20px;
    margin: 0;
  }

  .pokemon-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 20px;
  }

  .pokemon-card {
    background: white;
    border-radius: 12px;
    padding: 16px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    text-align: center;
    transition: transform 0.2s ease;
  }

  .pokemon-card:hover {
    transform: translateY(-5px);
  }

  .pokemon-card img {
    width: 120px;
    height: 120px;
    object-fit: contain;
  }

  .pokemon-card h2 {
    margin: 10px 0 5px;
    font-size: 20px;
    color: #333;
  }

  .pokemon-card p {
    margin: 4px 0;
    font-size: 14px;
    color: #555;
  }

  .add-button {
    display: inline-block;
    margin-top: 30px;
    padding: 10px 20px;
    background-color: #ff4757;
    color: white;
    border: none;
    border-radius: 8px;
    text-decoration: none;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .add-button:hover {
    background-color: #e84118;
  }
</style>

